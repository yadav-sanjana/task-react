'use client'
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button className='btn btn-secondary' onClick={() => console.log("clicked")}>Add To Card</button>
    </div>
  )
}

export default AddToCart
